<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Stayzio</title>
</head>
<link rel="stylesheet" href="/css/showlisting.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">  
<link href="https://api.mapbox.com/mapbox-gl-js/v3.14.0/mapbox-gl.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
<link rel="stylesheet" href="/css/footer.css">
<link rel="stylesheet" href="/css/output.css">
<script src="https://api.mapbox.com/mapbox-gl-js/v3.14.0/mapbox-gl.js"></script>
<link rel="icon" type="image/png" href="/images/favicon-removebg-preview.png">
 <script>
  const mapToken = "<%= process.env.MAP_TOKEN%>";
  const coordinates = <%-JSON.stringify(listing.geometry.coordinates)%>;
  const listingDataforMap = {
    title: "<%= listing.title %>",
    location: "<%= listing.location %>",
    country: "<%= listing.country %>"
  };
</script>

</head>

<body>
  <%- include("../includes/navbar.ejs") %>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    
    <!-- Header Actions -->
    <div class="action-header">
      <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center gap-4">
        <div class="flex-1">
          <h1 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-3"><%= listing.title %></h1>
          <div class="flex flex-wrap items-center gap-3">
            <div class="flex items-center gap-2 text-gray-600">
              <i class="fa-solid fa-location-dot text-[#fe424d]"></i>
              <span class="font-medium"><%= listing.location %>, <%= listing.country %></span>
            </div>
            <span class="tag capitalize">
              <i class="fa-solid fa-layer-group"></i> <%= listing.category %>
            </span>
          </div>
        </div>
        <div class="flex flex-wrap gap-2">
          <a href="/listings" class="btn-modern btn-outline-modern">
            <i class="fa-solid fa-arrow-left"></i> Back
          </a>
          <% if (currentUser && listing.owner._id.equals(currentUser._id)) { %>
            <a href="/listings/<%= listing._id %>/edit" class="btn-modern btn-outline-modern">
              <i class="fa-solid fa-pen-to-square"></i> Edit
            </a>
            <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST" class="inline">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
              <button type="submit" class="btn-modern bg-red-600 text-white hover:bg-red-700">
                <i class="fa-solid fa-trash"></i> Delete
              </button>
            </form>
          <% } %>
        </div>
      </div>
    </div>

    <!-- Hero Image -->
    <div class="hero-image mb-8">
      <img src="<%= listing.image.url %>" alt="<%= listing.title %>" class="w-full h-[500px] object-cover">
    </div>

    <!-- Content Grid -->
    <div class="grid lg:grid-cols-3 gap-8">
      
      <!-- Main Content -->
      <div class="lg:col-span-2 space-y-8">
        
      <!-- Host Info -->
<div class="property-card p-6">
  <h2 class="section-title">Your Host</h2>

  <div class="flex items-start gap-5">

    <!-- Host Image -->
    <img 
      src="<%= (listing.owner && listing.owner.profile && listing.owner.profile.photo && listing.owner.profile.photo.url) ? listing.owner.profile.photo.url : '/images/default-profile.png' %>"
      alt="Host Image"
      class="w-16 h-16 rounded-full object-cover shadow-md"
    />

    <div class="flex-1">

      <!-- Name -->
      <h3 class="font-bold text-gray-900 text-lg mb-1">
        <%= listing.owner?.hostDetails?.name || listing.owner?.username || "Unknown Host" %>
      </h3>

      <!-- Joined Date -->
      <% if (listing.owner?.hostDetails?.joinedAt) { %>
        <p class="text-gray-500 text-sm mb-3">
          Host since <%= new Date(listing.owner.hostDetails.joinedAt).toLocaleDateString("en-IN") %>
        </p>
      <% } %>

      <!-- Bio -->
      <% if (listing.owner?.hostDetails?.bio) { %>
        <p class="text-gray-700 leading-relaxed text-[15px] mb-3">
          <%= listing.owner.hostDetails.bio %>
        </p>
      <% } %>

      <!-- Contact Info -->
      <div class="flex flex-wrap gap-4 text-sm text-gray-600">
        
        <% if (listing.owner?.hostDetails?.phone) { %>
          <div class="flex items-center gap-2">
            <i class="fa-solid fa-phone text-gray-500"></i>
            <span><%= listing.owner.hostDetails.phone %></span>
          </div>
        <% } %>

        <% if (listing.owner?.email) { %>
          <div class="flex items-center gap-2">
            <i class="fa-solid fa-envelope text-gray-500"></i>
            <span><%= listing.owner.email %></span>
          </div>
        <% } %>

        <% if (listing.owner?.hostDetails?.country) { %>
          <div class="flex items-center gap-2">
            <i class="fa-solid fa-location-dot text-gray-500"></i>
            <span><%= listing.owner.hostDetails.country %></span>
          </div>
        <% } %>

      </div>

    </div>
  </div>
</div>

        <!-- Description -->
        <div class="property-card p-6">
          <h2 class="section-title">About This Property</h2>
          <p class="text-gray-700 leading-relaxed text-[15px]"><%= listing.description %></p>
        </div>

        <!-- Property Details -->
        <div class="property-card p-6">
          <h2 class="section-title">Property Details</h2>
          <div class="grid sm:grid-cols-2 gap-4">
            <div class="info-badge">
              <i class="fa-solid fa-location-dot"></i>
              <div>
                <div class="text-xs text-gray-500 font-medium">Location</div>
                <div class="font-semibold text-gray-900"><%= listing.location %></div>
              </div>
            </div>
            <div class="info-badge">
              <i class="fa-solid fa-flag"></i>
              <div>
                <div class="text-xs text-gray-500 font-medium">Country</div>
                <div class="font-semibold text-gray-900"><%= listing.country %></div>
              </div>
            </div>
            <div class="info-badge">
              <i class="fa-solid fa-layer-group"></i>
              <div>
                <div class="text-xs text-gray-500 font-medium">Category</div>
                <div class="font-semibold text-gray-900 capitalize"><%= listing.category %></div>
              </div>
            </div>
            <div class="info-badge">
              <i class="fa-solid fa-rupee-sign"></i>
              <div>
                <div class="text-xs text-gray-500 font-medium">Price per Night</div>
                <div class="font-semibold text-gray-900">₹ <%= listing.price ? listing.price.toLocaleString("en-IN") : 'N/A' %></div>
              </div>
            </div>
          </div>
        </div>

        <!-- Map Section -->
        <div class="property-card p-6">
          <div class="flex justify-between items-center mb-6">
            <h2 class="section-title mb-0">Where You'll Be</h2>
            <button id="loadMapBtn" class="btn-modern btn-primary-modern">
              <i class="fa-solid fa-map-location-dot"></i> Show Map
            </button>
          </div>
          <div id="map" style="display:none;"></div>
        </div>

      </div>

      <!-- Booking Sidebar -->
      <div class="lg:col-span-1">
        <div class="booking-panel p-6">
          <div class="price-tag">
            <div class="text-sm text-gray-600 font-semibold mb-1">Price per Night</div>
            <div class="text-4xl font-bold text-[#fe424d]">
              ₹ <%= listing.price ? listing.price.toLocaleString("en-IN") : 'N/A' %>
            </div>
          </div>

          <form id="booking-form" action="/bookings/details" method="GET" class="needs-validation" novalidate>
            <input type="hidden" name="listingId" value="<%= listing._id %>">
            
            <div class="mb-4">
              <label class="block text-sm font-semibold text-gray-700 mb-2">Check-in Date</label>
              <input type="text" id="checkIn" name="checkIn" class="input-modern" required placeholder="Select date">
            </div>
            
            <div class="mb-4">
              <label class="block text-sm font-semibold text-gray-700 mb-2">Check-out Date</label>
              <input type="text" id="checkOut" name="checkOut" class="input-modern" required placeholder="Select date">
            </div>

            <div id="date-error" class="text-red-600 text-sm text-center mb-4 font-semibold min-h-[1.25rem]"></div>

            <button type="submit" id="bookBtn" class="w-full btn-modern btn-primary-modern text-lg">
              <span class="button-text">
                <i class="fa-solid fa-calendar-check"></i> Reserve Now
              </span>
              <span class="button-loader"></span>
            </button>
          </form>
        </div>
      </div>
    </div>

    <!-- Reviews Section -->
    <div class="divider"></div>

    <div class="property-card p-8">
      <h2 class="section-title">Guest Reviews</h2>

      <% if (currentUser) { %>
        <div class="bg-gray-50 rounded-2xl p-6 mb-8 border border-gray-200">
          <h3 class="font-bold text-lg text-gray-900 mb-4">Share Your Experience</h3>
          <form action="/listings/<%= listing._id %>/reviews" method="POST" class="needs-validation" novalidate>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <div class="mb-5">
              <label class="block text-sm font-semibold text-gray-700 mb-3">Your Rating</label>
              <div class="star-rating flex gap-2">
                <i class="fa-solid fa-star" data-value="1"></i>
                <i class="fa-solid fa-star" data-value="2"></i>
                <i class="fa-solid fa-star" data-value="3"></i>
                <i class="fa-solid fa-star" data-value="4"></i>
                <i class="fa-solid fa-star" data-value="5"></i>
              </div>
              <input type="hidden" name="review[rating]" id="ratingValue" value="1" required>
            </div>
            
            <div class="mb-5">
              <label class="block text-sm font-semibold text-gray-700 mb-2">Your Review</label>
              <textarea id="comment" name="review[comment]" class="input-modern resize-none" rows="4" placeholder="Tell us about your stay..." required></textarea>
            </div>
            
            <button type="submit" class="btn-modern btn-primary-modern">
              <i class="fa-solid fa-paper-plane"></i> Submit Review
            </button>
          </form>
        </div>
      <% } else { %>
        <div class="bg-gradient-to-br from-gray-50 to-gray-100 rounded-2xl p-12 text-center border border-gray-200 mb-8">
          <div class="w-20 h-20 bg-white rounded-full flex items-center justify-center mx-auto mb-4 shadow-md">
            <i class="fa-solid fa-lock text-3xl text-gray-400"></i>
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-2">Login Required</h3>
          <p class="text-gray-600 mb-6">Please log in to share your review</p>
          <a href="/login" class="btn-modern btn-primary-modern">
            <i class="fa-solid fa-sign-in-alt"></i> Login to Continue
          </a>
        </div>
      <% } %>

      <% if (listing.reviews.length === 0) { %>
        <div class="text-center py-16">
          <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <i class="fa-regular fa-comments text-5xl text-gray-300"></i>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">No Reviews Yet</h3>
          <p class="text-gray-500">Be the first to share your experience!</p>
        </div>
      <% } else { %>
        <div class="grid md:grid-cols-2 gap-6">
          <% listing.reviews.forEach(review => { %>
            <div class="review-card">
              <div class="flex items-start gap-4 mb-4">

                <% if (review.author && review.author.username) { %>

                  <!-- Avatar with profile photo support (NEW) -->
                  <div class="avatar flex-shrink-0 relative overflow-hidden rounded-full">
                    <% if (review.author.profile && review.author.profile.photo && review.author.profile.photo.url) { %>
                      <!-- Show profile photo -->
                      <img 
                        src="<%= review.author.profile.photo.url %>"
                        alt="User Photo"
                        class="w-full h-full object-cover"
                      />
                    <% } else { %>
                      <!-- Fallback username initial (YOUR ORIGINAL DESIGN) -->
                      <span class="text-white font-bold text-lg flex items-center justify-center w-full h-full">
                        <%= review.author.username.charAt(0).toUpperCase() %>
                      </span>
                    <% } %>
                  </div>

                  <div class="flex-1">
                    <div class="font-bold text-gray-900 text-lg"><%= review.author.username %></div>
                    <div class="flex gap-1 mt-1">
                      <% for (let i = 1; i <= 5; i++) { %>
                        <i class="<%= i <= review.rating ? 'fa-solid fa-star' : 'fa-regular fa-star' %>" 
                          style="color: #ffc107; font-size: 0.875rem;"></i>
                      <% } %>
                    </div>
                  </div>

                <% } else { %>

                  <!-- Anonymous fallback -->
                  <div class="avatar flex-shrink-0 bg-gray-400">
                    <i class="fa-solid fa-user-secret"></i>
                  </div>

                  <div class="flex-1">
                    <div class="font-bold text-gray-900 text-lg italic">Anonymous</div>
                    <div class="flex gap-1 mt-1">
                      <% for (let i = 1; i <= 5; i++) { %>
                        <i class="<%= i <= review.rating ? 'fa-solid fa-star' : 'fa-regular fa-star' %>" 
                          style="color: #ffc107; font-size: 0.875rem;"></i>
                      <% } %>
                    </div>
                  </div>

                <% } %>
              </div>

              <!-- Comment -->
              <p class="text-gray-700 leading-relaxed mb-4"><%= review.comment %></p>

              <!-- Delete button for author -->
              <% if (currentUser && review.author && review.author._id.equals(currentUser._id)) { %>
                <form action="/listings/<%= listing._id %>/reviews/<%= review._id %>?_method=DELETE" method="POST">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button type="submit" class="text-red-600 text-sm font-semibold hover:text-red-700 transition">
                    <i class="fa-solid fa-trash"></i> Delete Review
                  </button>
                </form>
              <% } %>
            </div>

          <% }) %>
        </div>
      <% } %>
    </div>

  </div>

  <%- include("../includes/footer.ejs") %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
  <script src="/js/map.js"></script>
  <script src="/js/script.js"></script>

  <script>
    // Star Rating
    const stars = document.querySelectorAll('.star-rating .fa-star');
    const ratingInput = document.getElementById('ratingValue');

    if (stars.length > 0) {
      const updateStars = (rating) => {
        stars.forEach(star => {
          star.classList.toggle('active', star.getAttribute('data-value') <= rating);
        });
      };

      stars.forEach(star => {
        star.addEventListener('click', () => {
          const rating = star.getAttribute('data-value');
          ratingInput.value = rating;
          updateStars(rating);
        });
      });

      document.querySelector('.star-rating').addEventListener('mouseleave', () => {
        updateStars(ratingInput.value);
      });
      
      updateStars(ratingInput.value); // Initialize
    }

    // Map Toggle
    const mapBtn = document.getElementById('loadMapBtn');
    const mapDiv = document.getElementById('map');
    let mapInitialized = false;

    mapBtn.addEventListener('click', () => {
      const isHidden = mapDiv.style.display === 'none';
      mapDiv.style.display = isHidden ? 'block' : 'none';
      mapBtn.innerHTML = isHidden ? '<i class="fa-solid fa-eye-slash"></i> Hide Map' : '<i class="fa-solid fa-map-location-dot"></i> Show Map';

      if (isHidden && !mapInitialized) {
        initializeMap();
        mapInitialized = true;
      }
    });

    // Bootstrap Form Validation
    (() => {
      'use strict'
      const forms = document.querySelectorAll('.needs-validation')
      Array.from(forms).forEach(form => {
        form.addEventListener('submit', event => {
          if (!form.checkValidity()) {
            event.preventDefault();
            event.stopPropagation();
          }
          form.classList.add('was-validated');
        }, false)
      })
    })();

    // Flatpickr Initialization
    fetch(`/bookings/api/booked-dates?listingId=<%= listing._id %>`)
      .then(res => res.json())
      .then(data => {
        const disabledRanges = data.bookings.map(b => ({ from: b.checkIn, to: b.checkOut }));
        const flatpickrConfig = {
          dateFormat: "Y-m-d",
          altInput: true,
          altFormat: "d-m-Y",
          minDate: "today",
          disable: disabledRanges,
        };
        flatpickr("#checkIn", flatpickrConfig);
        flatpickr("#checkOut", flatpickrConfig);
      })
      .catch(err => {
        console.error("Failed to load booked dates:", err);
        // Fallback if API fails
        const fallbackConfig = {
            dateFormat: "Y-m-d",
            altInput: true,
            altFormat: "d-m-Y",
            minDate: "today"
        };
        flatpickr("#checkIn", fallbackConfig);
        flatpickr("#checkOut", fallbackConfig);
      });

const bookingForm = document.getElementById('booking-form');
const bookBtn = document.getElementById("bookBtn");

if (bookingForm) {
  bookingForm.addEventListener('submit', function(event) {
    const checkInInput = document.getElementById('checkIn');
    const checkOutInput = document.getElementById('checkOut');
    const dateError = document.getElementById('date-error');

    // Reset state
    dateError.textContent = ''; 
    checkInInput.classList.remove('is-invalid');
    checkOutInput.classList.remove('is-invalid');

    const checkInValue = checkInInput.value;
    const checkOutValue = checkOutInput.value;

    // 1️⃣ Empty date validation
    if (!checkInValue || !checkOutValue) {
      event.preventDefault();
      dateError.textContent = 'Please select both check-in and check-out dates.';
      if (!checkInValue) checkInInput.classList.add('is-invalid');
      if (!checkOutValue) checkOutInput.classList.add('is-invalid');
      return;
    }

    // 2️⃣ Check-out must be after check-in
    const checkInDate = new Date(checkInValue);
    const checkOutDate = new Date(checkOutValue);

    if (checkOutDate <= checkInDate) {
      event.preventDefault();
      dateError.textContent = 'Check-out date must be after the check-in date.';
      checkOutInput.classList.add('is-invalid');
      return;
    }

    // 3️⃣ Native HTML validation
    if (!bookingForm.checkValidity()) {
      event.preventDefault();
      bookingForm.reportValidity();
      return;
    }

    // 4️⃣ If everything is valid → show spinner + disable button
    bookBtn.classList.add("loading");
    bookBtn.disabled = true;
  });
}
</script>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<!-- Load your alert utilities -->
<script type="module" src="/utils/alerts.js"></script>

<script type="module">
  import { showError, showSuccess } from '/utils/alerts.js';

  const __serverError = <%- JSON.stringify(error && error.length > 0 ? error[0] : null) %>;
  const __serverSuccess = <%- JSON.stringify(success && success.length > 0 ? success[0] : null) %>;

  if (__serverError) showError('Error', __serverError);
  if (__serverSuccess) showSuccess('Success', __serverSuccess);
</script>
</body>
</html>