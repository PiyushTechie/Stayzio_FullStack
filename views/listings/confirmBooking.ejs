<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Confirm Your Booking - Stayzio</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    :root {
      --primary-color: #fe424d;
      --primary-dark: #e23940;
      --primary-light: #ff6b73;
      --gradient-primary: linear-gradient(135deg, #fe424d, #ff6b73);
      --shadow-soft: 0 10px 30px rgba(254, 66, 77, 0.1);
      --shadow-card: 0 8px 25px rgba(0, 0, 0, 0.08);
      --border-radius: 16px;
    }

    body {
      background: linear-gradient(135deg, #f8f9ff 0%, #fff5f5 100%);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      min-height: 100vh;
      padding: 2rem 0;
    }

    .main-container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    .page-header {
      background: var(--gradient-primary);
      color: white;
      border-radius: var(--border-radius);
      padding: 2.5rem 2rem;
      margin-bottom: 2rem;
      position: relative;
      overflow: hidden;
    }

    .page-header::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -20%;
      width: 200px;
      height: 200px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 50%;
    }

    .page-header::after {
      content: '';
      position: absolute;
      bottom: -30%;
      left: -10%;
      width: 150px;
      height: 150px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 50%;
    }

    .header-content {
      position: relative;
      z-index: 2;
    }

    .page-title {
      font-size: 2.2rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .page-subtitle {
      opacity: 0.9;
      font-size: 1.1rem;
      margin-bottom: 0;
    }

    .confirmation-card {
      background: white;
      border: 2px solid #f0f0f0;
      border-radius: var(--border-radius);
      margin-bottom: 2rem;
      overflow: hidden;
      transition: all 0.3s ease;
      box-shadow: var(--shadow-card);
      position: relative;
    }

    .confirmation-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: var(--gradient-primary);
    }

    .confirmation-card:hover {
      border-color: var(--primary-color);
      box-shadow: var(--shadow-soft);
    }

    .card-header-custom {
      background: linear-gradient(135deg, #f8f9ff, #fff);
      padding: 1.5rem 2rem;
      border-bottom: 2px solid #f0f0f0;
    }

    .card-title-custom {
      font-size: 1.4rem;
      font-weight: 700;
      color: var(--primary-color);
      margin-bottom: 0;
      display: flex;
      align-items: center;
      gap: 0.75rem;
    }

    .card-body-custom {
      padding: 2rem;
    }

    .listing-info {
      display: grid;
      grid-template-columns: 1fr auto;
      gap: 1.5rem;
      align-items: center;
    }

    .listing-details h4 {
      font-size: 1.5rem;
      font-weight: 700;
      color: #212529;
      margin-bottom: 0.5rem;
    }

    .listing-location {
      color: #6c757d;
      font-size: 1rem;
      margin-bottom: 1rem;
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .listing-price {
      background: var(--gradient-primary);
      color: white;
      padding: 1rem 1.5rem;
      border-radius: 12px;
      font-size: 1.2rem;
      font-weight: 700;
      text-align: center;
      min-width: 150px;
    }

    .detail-item {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      padding: 1rem 0;
      border-bottom: 1px solid #e9ecef;
    }

    .detail-item:last-child {
      border-bottom: none;
    }

    .detail-label {
      font-weight: 600;
      color: #495057;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      min-width: 120px;
    }

    .detail-value {
      font-weight: 600;
      color: #212529;
      text-align: right;
      flex: 1;
    }

    .guest-list {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 1rem;
      margin-top: 0.5rem;
      border-left: 4px solid var(--primary-color);
    }

    .guest-item {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.5rem 0;
      border-bottom: 1px solid #e9ecef;
    }

    .guest-item:last-child {
      border-bottom: none;
    }

    .guest-icon {
      width: 30px;
      height: 30px;
      border-radius: 50%;
      background: var(--gradient-primary);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.8rem;
    }

    .price-breakdown {
      background: linear-gradient(135deg, #fff5f5, #f8f9ff);
      border-radius: var(--border-radius);
      padding: 2rem;
      border: 2px solid rgba(254, 66, 77, 0.1);
    }

    .price-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.75rem 0;
      border-bottom: 1px solid rgba(254, 66, 77, 0.1);
    }

    .price-item:last-child {
      border-bottom: none;
      margin-top: 0.5rem;
      padding-top: 1rem;
      border-top: 2px solid var(--primary-color);
    }

    .price-label {
      font-weight: 600;
      color: #495057;
    }

    .price-value {
      font-weight: 600;
      color: #212529;
      font-size: 1.1rem;
    }

    .price-total {
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--primary-color);
    }

    .confirm-section {
      background: white;
      border-radius: var(--border-radius);
      padding: 2rem;
      box-shadow: var(--shadow-card);
      border: 2px solid rgba(254, 66, 77, 0.1);
      position: sticky;
      bottom: 2rem;
    }

    .btn-submit {
      background-color: var(--primary-color);
      color: white;
      border: none;
      padding: 1rem 2rem;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.2s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      min-height: 50px;
      gap: 0.5rem;
    }

    .btn-submit:disabled {
      opacity: 0.7;
      cursor: not-allowed;
    }

    .button-loader {
      display: none;
      width: 20px;
      height: 20px;
      border: 2px solid rgba(255, 255, 255, 0.3);
      border-top-color: #ffffff;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      position: absolute;
    }

    .btn-submit.loading .button-text {
      visibility: hidden;
      opacity: 0;
    }

    .btn-submit.loading .button-loader {
      display: block;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    .security-note {
      text-align: center;
      color: #6c757d;
      font-size: 0.9rem;
      margin-top: 1rem;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }

    .two-column {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 2rem;
      align-items: start;
    }

    @media (max-width: 992px) {
      .two-column { grid-template-columns: 1fr; gap: 1.5rem; }
      .confirm-section { position: static; }
    }

    @media (max-width: 768px) {
      .main-container { padding: 0 0.5rem; }
      .page-header { padding: 2rem 1.5rem; margin-bottom: 1.5rem; }
      .page-title { font-size: 1.8rem; }
      .card-body-custom { padding: 1.5rem; }
      .listing-info { grid-template-columns: 1fr; text-align: center; }
      .detail-item { flex-direction: column; align-items: flex-start; gap: 0.5rem; }
      .detail-value { text-align: left; }
    }
  </style>
</head>
<body>
  <div class="main-container">
    <div class="page-header">
      <div class="header-content">
        <h1 class="page-title">
          <i class="fas fa-clipboard-check"></i>
          Confirm Your Booking
        </h1>
        <p class="page-subtitle">Review your booking details before confirming</p>
      </div>
    </div>

    <div class="two-column">
      <div class="left-column">
        <!-- Property Details -->
        <div class="confirmation-card">
          <div class="card-header-custom">
            <h3 class="card-title-custom">
              <i class="fas fa-home"></i>
              Property Details
            </h3>
          </div>
          <div class="card-body-custom">
            <div class="listing-info">
              <div class="listing-details">
                <h4><%= listing.title %></h4>
                <p class="listing-location"><i class="fas fa-map-marker-alt"></i> <%= listing.location %></p>
              </div>
              <div class="listing-price">
                <div>₹<%= listing.price %></div>
                <small>per night</small>
              </div>
            </div>
          </div>
        </div>

        <!-- Booking Details -->
        <div class="confirmation-card">
          <div class="card-header-custom">
            <h3 class="card-title-custom">
              <i class="fas fa-calendar-alt"></i>
              Booking Details
            </h3>
          </div>
          <div class="card-body-custom">
            <div class="detail-item">
              <span class="detail-label"><i class="fas fa-calendar-plus"></i> Check-in</span>
              <span class="detail-value"><%= new Date(checkIn).toLocaleDateString('en-IN', { day:'2-digit', month:'short', year:'numeric' }) %></span>
            </div>
            <div class="detail-item">
              <span class="detail-label"><i class="fas fa-calendar-minus"></i> Check-out</span>
              <span class="detail-value"><%= new Date(checkOut).toLocaleDateString('en-IN', { day:'2-digit', month:'short', year:'numeric' }) %></span>
            </div>
            <div class="detail-item">
              <span class="detail-label"><i class="fas fa-moon"></i> Nights</span>
              <span class="detail-value"><%= numberOfNights %></span>
            </div>
            <div class="detail-item">
              <span class="detail-label"><i class="fas fa-users"></i> Guests</span>
              <div class="detail-value">
                <% if (guests && guests.length) { %>
                  <div class="guest-list">
                    <% guests.forEach((guest) => { %>
                      <div class="guest-item">
                        <div class="guest-icon">
                          <i class="fas <%= guest.type === 'Adult' ? 'fa-user-tie' : 'fa-child' %>"></i>
                        </div>
                        <div>
                          <strong><%= guest.name %></strong><br>
                          <small><%= guest.type %>, Age <%= guest.age %>, <%= guest.gender %></small>
                        </div>
                      </div>
                    <% }) %>
                  </div>
                <% } else { %>
                  <span>No guest details provided</span>
                <% } %>
              </div>
            </div>
            <div class="detail-item">
              <span class="detail-label"><i class="fas fa-phone"></i> Contact</span>
              <span class="detail-value"><%= contactNumber %></span>
            </div>
          </div>
        </div>
      </div>

      <div class="right-column">
        <!-- Price Breakdown -->
        <div class="confirmation-card">
          <div class="card-header-custom">
            <h3 class="card-title-custom"><i class="fas fa-receipt"></i> Price Breakdown</h3>
          </div>
          <div class="card-body-custom">
            <div class="price-breakdown">
              <% if (perNightPrices && perNightPrices.length) { %>
                <% perNightPrices.forEach((night) => {
                  let label = new Date(night.date).toLocaleDateString('en-IN', { weekday:'short', day:'2-digit', month:'short' });
                  let multiplierTag = '';
                  if (night.isWeekend) multiplierTag += ' (Weekend +20%)';
                  if (night.seasonalMultiplier && night.seasonalMultiplier !== 1) multiplierTag += ` (Seasonal x${night.seasonalMultiplier})`;
                  if (night.demandMultiplier && night.demandMultiplier !== 1) multiplierTag += ` (Demand x${night.demandMultiplier})`;
                %>
                  <div class="price-item">
                    <span class="price-label"><%= label + multiplierTag %></span>
                    <span class="price-value">₹<%= night.price.toFixed(2) %></span>
                  </div>
                <% }) %>
              <% } %>

              <div class="price-item">
                <span class="price-label">Subtotal (<%= numberOfNights %> nights)</span>
                <span class="price-value">₹<%= subtotal.toFixed(2) %></span>
              </div>
              <div class="price-item">
                <span class="price-label">GST (18%)</span>
                <span class="price-value">₹<%= gst.toFixed(2) %></span>
              </div>
              <div class="price-item">
                <span class="price-label">Convenience Fee (5%)</span>
                <span class="price-value">₹<%= convenienceFee.toFixed(2) %></span>
              </div>
              <div class="price-item">
                <span class="price-label price-total">Total Amount</span>
                <span class="price-value price-total">₹<%= finalTotal.toFixed(2) %></span>
              </div>
            </div>
          </div>
        </div>

        <!-- Confirm Section -->
        <div class="confirm-section">
          <form id="paymentForm" action="/bookings" method="POST">
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <button type="submit" class="btn-submit" id="confirmBtn">
              <span class="button-text"><i class="fas fa-credit-card"></i> Confirm & Pay ₹<%= finalTotal.toFixed(2) %></span>
              <div class="button-loader"></div>
            </button>
            <p class="security-note"><i class="fas fa-shield-alt"></i> Your payment is secured with 256-bit SSL encryption</p>
          </form>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const confirmBtn = document.getElementById("confirmBtn");
    const form = document.getElementById("paymentForm");

    form.addEventListener("submit", () => {
      confirmBtn.classList.add("loading");
      confirmBtn.disabled = true;
    });
  </script>
</body>
</html>
