<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Confirmed - Stayzio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #fe424d;
            --primary-dark: #e23940;
            --primary-light: #ff6b73;
            --success-color: #28a745;
            --success-light: #34d058;
            --gradient-primary: linear-gradient(135deg, #fe424d, #ff6b73);
            --gradient-success: linear-gradient(135deg, #28a745, #34d058);
            --shadow-soft: 0 10px 30px rgba(254, 66, 77, 0.1);
            --shadow-card: 0 20px 40px rgba(0, 0, 0, 0.1);
            --border-radius: 20px;
        }

        body {
            background: linear-gradient(135deg, #f8f9ff 0%, #fff5f5 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 0;
        }

        .confirmation-container {
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-card);
            overflow: hidden;
            max-width: 700px;
            width: 100%;
            margin: 0 auto;
            position: relative;
        }

        .success-header {
            background: var(--gradient-success);
            color: white;
            padding: 3rem 2rem 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .success-header::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
        }

        .success-header::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: -10%;
            width: 150px;
            height: 150px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
        }

        .success-icon {
            position: relative;
            z-index: 2;
            display: inline-block;
            margin-bottom: 1rem;
        }

        .check-circle {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.2);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            animation: pulseSuccess 2s ease-in-out infinite;
        }

        .check-mark {
            font-size: 4rem;
            color: white;
            animation: checkmarkAppear 1s ease-in-out 0.5s both;
        }

        .success-title {
            font-size: 2.2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            position: relative;
            z-index: 2;
            animation: slideInUp 0.8s ease-out 0.3s both;
        }

        .success-subtitle {
            font-size: 1.1rem;
            opacity: 0.9;
            position: relative;
            z-index: 2;
            animation: slideInUp 0.8s ease-out 0.5s both;
        }

        .content-section {
            padding: 2.5rem 2rem;
        }

        .section-divider {
            height: 3px;
            background: var(--gradient-success);
            border: none;
            border-radius: 2px;
            margin: 2rem 0;
            opacity: 0.3;
            animation: expandWidth 1s ease-out 1s both;
        }

        .booking-summary {
            background: linear-gradient(135deg, #f8f9ff, #fff);
            border: 2px solid rgba(40, 167, 69, 0.1);
            border-radius: var(--border-radius);
            padding: 2rem;
            margin: 2rem 0;
            animation: slideInUp 0.8s ease-out 0.7s both;
        }

        .summary-title {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--success-color);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .summary-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
            border-bottom: 1px solid #e9ecef;
            animation: fadeInLeft 0.6s ease-out calc(1s + var(--delay, 0s)) both;
        }

        .summary-item:last-child {
            border-bottom: none;
        }

        .summary-label {
            font-weight: 600;
            color: #495057;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .summary-value {
            font-weight: 600;
            color: #212529;
        }

        .property-link {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 600;
        }

        .property-link:hover {
            color: var(--primary-dark);
        }

        .booking-id {
            background: #f8f9fa;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
            border: 2px solid #e9ecef;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            margin-top: 2rem;
            animation: slideInUp 0.8s ease-out 0.9s both;
        }

        .btn-primary {
            background: var(--gradient-primary);
            border: none;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.3s ease;
            color: white;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            color: white;
        }

        .btn-outline-secondary {
            border: 2px solid #6c757d;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 12px;
            transition: all 0.3s ease;
            color: #6c757d;
        }

        .btn-outline-secondary:hover {
            background: #6c757d;
            border-color: #6c757d;
            color: white;
        }

        .floating-particles {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            pointer-events: none;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            background: rgba(40, 167, 69, 0.6);
            border-radius: 50%;
            animation: floatUp 3s ease-in-out infinite;
        }

        .particle:nth-child(1) { width: 4px; height: 4px; left: 10%; animation-delay: 0s; }
        .particle:nth-child(2) { width: 6px; height: 6px; left: 20%; animation-delay: 0.5s; }
        .particle:nth-child(3) { width: 3px; height: 3px; left: 30%; animation-delay: 1s; }
        .particle:nth-child(4) { width: 5px; height: 5px; left: 40%; animation-delay: 1.5s; }
        .particle:nth-child(5) { width: 4px; height: 4px; left: 50%; animation-delay: 2s; }
        .particle:nth-child(6) { width: 6px; height: 6px; left: 60%; animation-delay: 2.5s; }
        .particle:nth-child(7) { width: 3px; height: 3px; left: 70%; animation-delay: 0.8s; }
        .particle:nth-child(8) { width: 5px; height: 5px; left: 80%; animation-delay: 1.3s; }
        .particle:nth-child(9) { width: 4px; height: 4px; left: 90%; animation-delay: 1.8s; }

        @keyframes pulseSuccess {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.1); opacity: 0.8; }
        }

        @keyframes checkmarkAppear {
            0% { transform: scale(0) rotate(45deg); opacity: 0; }
            50% { transform: scale(1.2) rotate(45deg); opacity: 1; }
            100% { transform: scale(1) rotate(0deg); opacity: 1; }
        }

        @keyframes slideInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeInLeft {
            from { opacity: 0; transform: translateX(-20px); }
            to { opacity: 1; transform: translateX(0); }
        }

        @keyframes expandWidth {
            from { width: 0; }
            to { width: 100%; }
        }

        @keyframes floatUp {
            0% {
                opacity: 0;
                transform: translateY(100vh) translateX(0px) rotate(0deg);
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                opacity: 0;
                transform: translateY(-100px) translateX(20px) rotate(360deg);
            }
        }

        @media (max-width: 768px) {
            .confirmation-container {
                margin: 1rem;
            }
            
            .success-header {
                padding: 2.5rem 1.5rem 1.5rem;
            }
            
            .content-section {
                padding: 2rem 1.5rem;
            }
            
            .success-title {
                font-size: 1.8rem;
            }

            .action-buttons {
                flex-direction: column;
            }

            .check-circle {
                width: 100px;
                height: 100px;
            }

            .check-mark {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="confirmation-container">
            <div class="floating-particles">
                <div class="particle"></div>
                <div class="particle"></div>
                <div class="particle"></div>
                <div class="particle"></div>
                <div class="particle"></div>
                <div class="particle"></div>
                <div class="particle"></div>
                <div class="particle"></div>
                <div class="particle"></div>
            </div>

            <div class="success-header">
                <div class="success-icon">
                    <div class="check-circle">
                        <i class="fas fa-check check-mark"></i>
                    </div>
                </div>
                <h1 class="success-title">Booking Confirmed!</h1>
                <p class="success-subtitle">
                    Thank you for booking with Stayzio. We've sent a confirmation to your email.
                </p>
            </div>

            <div class="content-section">
                <hr class="section-divider">

                <% if (booking && booking.listing) { %>
                <div class="booking-summary">
                    <h4 class="summary-title">
                        <i class="fas fa-clipboard-list"></i>
                        Booking Summary
                    </h4>
                    
                    <div class="summary-item" style="--delay: 0.1s">
                        <span class="summary-label">
                            <i class="fas fa-home"></i>
                            Property
                        </span>
                        <span class="summary-value">
                            <a href="/listings/<%= booking.listing._id %>" class="property-link">
                                <%= booking.listing.title %>
                            </a>
                        </span>
                    </div>

                    <div class="summary-item" style="--delay: 0.2s">
                        <span class="summary-label">
                            <i class="fas fa-calendar-plus"></i>
                            Check-in
                        </span>
                        <span class="summary-value">
                            <%= new Date(booking.checkIn).toLocaleDateString() %>
                        </span>
                    </div>

                    <div class="summary-item" style="--delay: 0.3s">
                        <span class="summary-label">
                            <i class="fas fa-calendar-minus"></i>
                            Check-out
                        </span>
                        <span class="summary-value">
                            <%= new Date(booking.checkOut).toLocaleDateString() %>
                        </span>
                    </div>

                    <div class="summary-item" style="--delay: 0.4s">
                        <span class="summary-label">
                            <i class="fas fa-receipt"></i>
                            Booking ID
                        </span>
                        <span class="summary-value">
                            <code class="booking-id"><%= booking._id %></code>
                        </span>
                    </div>
                </div>
                <% } %>
                
                <div class="action-buttons">
                    <a href="/bookings/my" class="btn btn-primary">
                        <i class="fas fa-list me-2"></i>
                        View All My Bookings
                    </a>
                    <a href="/listings" class="btn btn-outline-secondary">
                        <i class="fas fa-search me-2"></i>
                        Continue Exploring
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>