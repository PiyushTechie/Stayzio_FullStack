<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guest Details - Stayzio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #fe424d;
            --primary-dark: #e23940;
            --primary-light: #fff5f5;
            --text-dark: #2d3748;
            --text-light: #718096;
            --border-color: #e2e8f0;
            --bg-light: #f7fafc;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #ffffff;
            color: var(--text-dark);
            line-height: 1.6;
        }

        .page-container {
            min-height: 100vh;
            padding: 0;
        }

        .header-section {
            background-color: var(--primary-color);
            color: white;
            padding: 2rem 0;
        }

        .header-content {
            padding: 0 2rem;
        }

        .header-title {
            font-size: 2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            text-align: left;
        }

        .header-subtitle {
            font-size: 1.1rem;
            opacity: 0.9;
            margin: 0;
            text-align: left;
        }

        .main-content {
            padding: 2rem;
            background-color: #ffffff;
        }

        .form-section {
            max-width: none;
            width: 100%;
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 1.5rem;
            text-align: left;
            border-bottom: 2px solid var(--primary-color);
            padding-bottom: 0.5rem;
            display: inline-block;
        }

        .guest-counter-section {
            background-color: var(--bg-light);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 2rem;
        }

        .form-label {
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.95rem;
        }

        .form-label i {
            color: var(--primary-color);
            width: 16px;
        }

        .form-control {
            border: 2px solid var(--border-color);
            border-radius: 6px;
            padding: 0.75rem;
            font-size: 1rem;
            background-color: #ffffff;
            transition: border-color 0.2s ease;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(254, 66, 77, 0.25);
            outline: none;
        }

        .form-select {
            border: 2px solid var(--border-color);
            border-radius: 6px;
            padding: 0.75rem;
            font-size: 1rem;
            background-color: #ffffff;
            transition: border-color 0.2s ease;
        }

        .form-select:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 0.2rem rgba(254, 66, 77, 0.25);
            outline: none;
        }

        .guest-counter {
            text-align: center;
            font-weight: 600;
            font-size: 1.1rem;
        }

        .guest-card {
            background-color: #ffffff;
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            border-left: 4px solid var(--primary-color);
        }

        .guest-card-header {
            color: var(--primary-color);
            font-weight: 700;
            margin-bottom: 1.5rem;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .guest-type-icon {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background-color: var(--primary-color);
            color: white;
            border-radius: 6px;
            font-size: 1.1rem;
        }

        .guest-number-badge {
            background-color: var(--primary-light);
            color: var(--primary-color);
            border-radius: 4px;
            padding: 0.25rem 0.5rem;
            font-size: 0.85rem;
            font-weight: 600;
            margin-left: auto;
        }

        .contact-section {
            background-color: var(--bg-light);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            padding: 1.5rem;
            margin: 2rem 0;
        }

        .btn-submit {
            background-color: var(--primary-color);
            color: white;
            border: none;
            padding: 1rem 2rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.2s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            position: relative;
            min-height: 50px;
        }

        .btn-submit:disabled {
            opacity: 0.7;
            cursor: not-allowed;
        }

        .button-loader {
            display: none;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-top-color: #ffffff;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        .btn-submit.loading .button-text {
            visibility: hidden;
            opacity: 0;
        }

        .btn-submit.loading .button-loader {
            display: block;
            position: absolute;
        }

        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .header-content {
                padding: 0 1rem;
            }

            .main-content {
                padding: 1.5rem 1rem;
            }

            .header-title {
                font-size: 1.5rem;
            }

            .header-subtitle {
                font-size: 1rem;
            }

            .guest-counter-section {
                padding: 1rem;
            }

            .guest-card {
                padding: 1rem;
            }

            .contact-section {
                padding: 1rem;
            }

            .btn-submit {
                width: 100%;
                padding: 1rem;
            }

            .guest-card-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }

            .guest-number-badge {
                margin-left: 0;
                align-self: flex-start;
            }
        }

        @media (max-width: 480px) {
            .header-content {
                padding: 0 0.75rem;
            }

            .main-content {
                padding: 1rem 0.75rem;
            }

            .section-title {
                font-size: 1.3rem;
            }

            .form-control,
            .form-select {
                padding: 0.625rem;
                font-size: 0.95rem;
            }

            .guest-counter-section {
                padding: 0.75rem;
            }

            .guest-card {
                padding: 0.75rem;
            }

            .contact-section {
                padding: 0.75rem;
            }
        }

        .invalid-feedback {
            display: block;
            width: 100%;
            margin-top: 0.25rem;
            font-size: 0.875rem;
            color: #dc3545;
        }

        .was-validated .form-control:invalid {
            border-color: #dc3545;
            box-shadow: 0 0 0 0.2rem rgba(220, 53, 69, 0.25);
        }

        .was-validated .form-select:invalid {
            border-color: #dc3545;
            box-shadow: 0 0 0 0.2rem rgba(220, 53, 69, 0.25);
        }
    </style>
</head>
<body>
    <div class="page-container">
        <section class="header-section">
            <div class="header-content">
                <h1 class="header-title">Guest Details</h1>
                <p class="header-subtitle">Enter the details for all guests who will be staying</p>
            </div>
        </section>

        <section class="main-content">
            <div class="form-section">
                <form action="/bookings/details" method="POST" class="needs-validation" novalidate>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <h2 class="section-title">Number of Guests</h2>
                    
                    <div class="guest-counter-section">
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label for="adults" class="form-label">
                                    <i class="fas fa-user-tie"></i>
                                    Adults (18+)
                                </label>
                                <input type="number" id="adults" class="form-control guest-counter" value="1" min="1" max="8" required>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label for="children" class="form-label">
                                    <i class="fas fa-child"></i>
                                    Children (0-17)
                                </label>
                                <input type="number" id="children" class="form-control guest-counter" value="0" min="0" max="6" required>
                            </div>
                        </div>
                    </div>

                    <h2 class="section-title">Guest Information</h2>
                    
                    <div id="guest-fields-container"></div>

                    <div class="contact-section">
                        <label for="contactNumber" class="form-label">
                            <i class="fas fa-phone"></i>
                            Contact Mobile Number
                        </label>
                        <input type="tel" id="contactNumber" name="contactNumber" class="form-control" placeholder="e.g., 9876543210" required>
                        <div class="invalid-feedback">Please provide a contact number.</div>
                    </div>

                    <div>
                        <button type="submit" class="btn-submit">
                            <span class="button-text">
                                <i class="fas fa-arrow-right"></i>
                                Proceed to Confirmation
                            </span>
                            <div class="button-loader"></div>
                        </button>
                    </div>
                </form>
            </div>
        </section>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
<script>
    // ================= CONFIGURATION =================
    const MAX_ADULTS = 8;
    const MAX_CHILDREN = 5;
    const MAX_TOTAL_GUESTS = 13;

    // ================= ELEMENTS =================
    const adultsInput = document.getElementById('adults');
    const childrenInput = document.getElementById('children');
    const container = document.getElementById('guest-fields-container');
    const form = document.querySelector('form');
    const submitButton = document.querySelector('.btn-submit');

    // ================= LOADER FUNCTIONS =================
    function showLoader() {
        submitButton.classList.add('loading');
        submitButton.disabled = true;
    }

    function hideLoader() {
        submitButton.classList.remove('loading');
        submitButton.disabled = false;
    }

    // ================= GUEST HANDLING =================
    function handleGuestChange(event) {
        let adults = parseInt(adultsInput.value, 10) || 0;
        let children = parseInt(childrenInput.value, 10) || 0;

        // Enforce individual limits
        if (adults > MAX_ADULTS) {
            adultsInput.value = MAX_ADULTS;
            adults = MAX_ADULTS;
        }
        if (children > MAX_CHILDREN) {
            childrenInput.value = MAX_CHILDREN;
            children = MAX_CHILDREN;
        }

        // Enforce total limit
        if (adults + children > MAX_TOTAL_GUESTS) {
            alert(`You cannot select more than ${MAX_TOTAL_GUESTS} guests in total.`);
            const overage = (adults + children) - MAX_TOTAL_GUESTS;
            event.target.value = parseInt(event.target.value, 10) - overage;
        }

        generateGuestFields();
    }

    function generateGuestFields() {
        container.innerHTML = '';
        const adults = parseInt(adultsInput.value) || 0;
        const children = parseInt(childrenInput.value) || 0;

        // Adults
        for (let i = 0; i < adults; i++) {
            container.appendChild(createGuestFieldElement('Adult', i));
        }

        // Children
        for (let i = 0; i < children; i++) {
            container.appendChild(createGuestFieldElement('Child', i));
        }
    }

    function createGuestFieldElement(type, index) {
        const offsetIndex = type === 'Adult' ? index : parseInt(adultsInput.value) + index;
        const div = document.createElement('div');
        div.className = 'guest-card';

        const icon = type === 'Adult' ? 'fa-user-tie' : 'fa-child';

        div.innerHTML = `
            <div class="guest-card-header">
                <span class="guest-type-icon">
                    <i class="fas ${icon}"></i>
                </span>
                ${type} ${index + 1}
                <span class="guest-number-badge">#${index + 1}</span>
            </div>
            <input type="hidden" name="guests[${offsetIndex}][type]" value="${type}">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label class="form-label">
                        <i class="fas fa-signature"></i>
                        Full Name
                    </label>
                    <input type="text" name="guests[${offsetIndex}][name]" class="form-control" required>
                </div>
                <div class="col-md-3 mb-3">
                    <label class="form-label">
                        <i class="fas fa-birthday-cake"></i>
                        Age
                    </label>
                    <input type="number" name="guests[${offsetIndex}][age]" class="form-control"
                        min="${type === 'Adult' ? 18 : 0}" max="${type === 'Adult' ? 120 : 17}" required>
                </div>
                <div class="col-md-3 mb-3">
                    <label class="form-label">
                        <i class="fas fa-venus-mars"></i>
                        Gender
                    </label>
                    <select name="guests[${offsetIndex}][gender]" class="form-select" required>
                        <option value="" selected disabled>Select...</option>
                        <option value="Male">Male</option>
                        <option value="Female">Female</option>
                        <option value="Other">Other</option>
                    </select>
                </div>
            </div>
        `;
        return div;
    }

    // ================= FORM VALIDATION =================
    (function () {
        'use strict';
        window.addEventListener('load', function () {
            var forms = document.getElementsByClassName('needs-validation');
            Array.prototype.filter.call(forms, function (form) {
                form.addEventListener('submit', function (event) {
                    if (form.checkValidity() === false) {
                        event.preventDefault();
                        event.stopPropagation();
                    } else {
                        // ✅ Show loader on valid submit
                        showLoader();
                    }
                    form.classList.add('was-validated');
                }, false);
            });
        }, false);
    })();

    adultsInput.addEventListener('input', handleGuestChange);
    childrenInput.addEventListener('input', handleGuestChange);
    generateGuestFields();
</script>



</body>
</html>