<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Bookings - Stayzio</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #fe424d;
            --primary-dark: #e23940;
            --primary-light: #ff6b73;
            --gradient-primary: linear-gradient(135deg, #fe424d, #ff6b73);
            --shadow-soft: 0 10px 30px rgba(254, 66, 77, 0.1);
            --shadow-card: 0 8px 25px rgba(0, 0, 0, 0.08);
            --border-radius: 16px;
        }

        body {
            background: linear-gradient(135deg, #f8f9ff 0%, #fff5f5 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            padding: 2rem 0;
        }

        .main-container {
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--shadow-card);
            margin: 0 auto;
            overflow: hidden;
            max-width: 1200px;
            position: relative;
        }

        .header-section {
            background: var(--gradient-primary);
            color: white;
            padding: 2.5rem 2rem;
            position: relative;
            overflow: hidden;
        }

        .header-section::before {
            content: '';
            position: absolute;
            top: -50%;
            right: -20%;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
        }

        .header-section::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: -10%;
            width: 150px;
            height: 150px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
        }

        .header-content {
            position: relative;
            z-index: 2;
        }

        .header-title {
            font-size: 2.2rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .header-subtitle {
            opacity: 0.9;
            font-size: 1.1rem;
            margin-bottom: 0;
        }

        .content-section {
            padding: 2.5rem 2rem;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: #6c757d;
        }

        .empty-icon {
            font-size: 4rem;
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            opacity: 0.6;
        }

        .empty-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .empty-subtitle {
            font-size: 1.1rem;
            margin-bottom: 2rem;
        }

        .booking-card {
            background: white;
            border: 2px solid #f0f0f0;
            border-radius: var(--border-radius);
            margin-bottom: 1.5rem;
            overflow: hidden;
            transition: all 0.3s ease;
            position: relative;
        }

        .booking-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--gradient-primary);
        }

        .booking-card:hover {
            border-color: var(--primary-color);
            box-shadow: var(--shadow-soft);
        }

        .booking-header {
            padding: 1.5rem;
            background: linear-gradient(135deg, #f8f9ff, #fff);
            border-bottom: 1px solid #e9ecef;
        }

        .listing-title {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--primary-color);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 0;
        }

        .listing-title:hover {
            color: var(--primary-dark);
        }

        .booking-body {
            padding: 1.5rem;
        }

        .booking-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .detail-item {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
        }

        .detail-label {
            font-size: 0.9rem;
            font-weight: 600;
            color: #6c757d;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .detail-value {
            font-size: 1rem;
            font-weight: 600;
            color: #495057;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 600;
            text-transform: capitalize;
        }

        .status-confirmed {
            background: linear-gradient(135deg, #28a745, #34d058);
            color: white;
        }

        .status-pending {
            background: linear-gradient(135deg, #ffc107, #ffed4e);
            color: #8b5a00;
        }

        .status-cancelled {
            background: linear-gradient(135deg, #dc3545, #fd7e7e);
            color: white;
        }

        .booking-actions {
            display: flex;
            justify-content: flex-end;
            padding-top: 1rem;
            border-top: 1px solid #e9ecef;
        }

        .btn-cancel {
            background: linear-gradient(135deg, #dc3545, #fd7e7e);
            border: none;
            padding: 0.75rem 1.5rem;
            color: white;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .btn-cancel:hover {
            background: linear-gradient(135deg, #c82333, #e55a5a);
            color: white;
        }

        .no-action {
            color: #6c757d;
            font-style: italic;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background: linear-gradient(135deg, #fff, #f8f9ff);
            border: 2px solid rgba(254, 66, 77, 0.1);
            border-radius: var(--border-radius);
            padding: 1.5rem;
            text-align: center;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 700;
            color: var(--primary-color);
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: #6c757d;
            font-weight: 600;
        }

        @media (max-width: 768px) {
            .main-container {
                margin: 0 1rem;
            }
            
            .header-section {
                padding: 2rem 1.5rem;
            }
            
            .content-section {
                padding: 2rem 1.5rem;
            }
            
            .header-title {
                font-size: 1.8rem;
            }

            .booking-details {
                grid-template-columns: 1fr;
                gap: 1rem;
            }

            .stats-container {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="main-container">
            <div class="header-section">
                <div class="header-content">
                    <h1 class="header-title">
                        <i class="fas fa-calendar-check"></i>
                        My Bookings
                    </h1>
                    <p class="header-subtitle">Track and manage all your reservations</p>
                </div>
            </div>

            <div class="content-section">
                <% const now = new Date(); %>
                <% if (!bookings || bookings.length === 0) { %>
                    <div class="empty-state">
                        <div class="empty-icon">
                            <i class="fas fa-calendar-times"></i>
                        </div>
                        <h3 class="empty-title">No Bookings Yet</h3>
                        <p class="empty-subtitle">Start exploring amazing places and make your first reservation!</p>
                    </div>
                <% } else { %>
                    <% bookings.forEach(b => { %>
                        <div class="booking-card">
                            <div class="booking-header">
                                <a href="/listings/<%= b.listing._id %>" class="listing-title">
                                    <i class="fas fa-home"></i>
                                    <%= b.listing.title %>
                                </a>
                            </div>
                            <div class="booking-body">
                                <div class="booking-details">
                                    <div class="detail-item">
                                        <span class="detail-label">
                                            <i class="fas fa-calendar-plus"></i>
                                            Check-in
                                        </span>
                                        <span class="detail-value"><%= b.checkIn.toDateString() %></span>
                                    </div>
                                    <div class="detail-item">
                                        <span class="detail-label">
                                            <i class="fas fa-calendar-minus"></i>
                                            Check-out
                                        </span>
                                        <span class="detail-value"><%= b.checkOut.toDateString() %></span>
                                    </div>
                                    <div class="detail-item">
                                        <span class="detail-label">
                                            <i class="fas fa-info-circle"></i>
                                            Status
                                        </span>
                                        <span class="status-badge status-<%= b.status %>">
                                            <% if (b.status === 'confirmed') { %>
                                                <i class="fas fa-check-circle"></i>
                                            <% } else if (b.status === 'pending') { %>
                                                <i class="fas fa-clock"></i>
                                            <% } else if (b.status === 'cancelled') { %>
                                                <i class="fas fa-times-circle"></i>
                                            <% } %>
                                            <%= b.status %>
                                        </span>
                                    </div>
                                </div>
                                <div class="booking-actions">
                                    <% if (b.status !== "cancelled" && b.checkIn > now) { %>
                                        <form action="/bookings/<%= b._id %>/cancel" method="POST" class="cancel-form" style="display: inline;">
                                            <button type="button" class="btn-cancel" data-bs-toggle="modal" data-bs-target="#cancelModal">
                                                <i class="fas fa-times"></i>
                                                Cancel Booking
                                            </button>
                                        </form>
                                    <% } else { %>
                                        <span class="no-action">No actions available</span>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } %>
            </div>
        </div>
    </div>

    <div class="modal fade" id="cancelModal" tabindex="-1" aria-labelledby="cancelModalLabel" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="cancelModalLabel">
              <i class="fas fa-exclamation-triangle text-danger me-2"></i> Confirm Cancellation
            </h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body">
            Are you sure you want to cancel this booking? This action cannot be undone.
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">No, Keep It</button>
            <button type="button" class="btn btn-danger" id="confirmCancelBtn">Yes, Cancel Booking</button>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        document.addEventListener("DOMContentLoaded", function() {
            // Get the modal and the confirm button from the DOM
            const cancelModal = document.getElementById('cancelModal');
            const confirmCancelBtn = document.getElementById('confirmCancelBtn');

            // A variable to store the form that needs to be submitted
            let formToSubmit = null;

            // Listen for when the modal is about to be shown
            cancelModal.addEventListener('show.bs.modal', function (event) {
                // event.relatedTarget is the button that was clicked to open the modal
                const button = event.relatedTarget;
                
                // Find the closest parent form of the clicked button
                formToSubmit = button.closest('.cancel-form');
            });

            // Add a click listener to the "Yes, Cancel Booking" button inside the modal
            confirmCancelBtn.addEventListener('click', function () {
                // If we have a form stored, submit it
                if (formToSubmit) {
                    formToSubmit.submit();
                }
            });
        });
    </script>
</body>
</html>