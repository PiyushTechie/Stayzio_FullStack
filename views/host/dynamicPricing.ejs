<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dynamic Pricing - Stayzio</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  
<style>
  :root {
    --brand-primary: #fe424d;
    --brand-primary-dark: #e63946;
    --brand-primary-light: #ff6b74;
    --text-primary: #1a1a1a;
    --text-secondary: #6c757d;
    --bg-light: #f8f9fa;
    --border-color: #e9ecef;
  }

  body {
    background: linear-gradient(135deg, #fff5f5 0%, #ffffff 100%);
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  }

  .container {
    padding-top: 2rem;
    padding-bottom: 2rem;
  }

  h1 {
    font-size: 2rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
    position: relative;
    padding-bottom: 1rem;
  }

  h1::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, var(--brand-primary), var(--brand-primary-light));
    border-radius: 2px;
  }

  .empty-state {
    background: white;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(254, 66, 77, 0.08);
    padding: 4rem 2rem;
    text-align: center;
    margin-top: 2rem;
    border: 1px solid var(--border-color);
  }

  .empty-state-icon {
    font-size: 4rem;
    margin-bottom: 1rem;
    opacity: 0.4;
  }

  .empty-state p {
    color: var(--text-secondary);
    font-size: 1.1rem;
    margin: 0;
  }

  .table-container {
    background: white;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(254, 66, 77, 0.08);
    overflow: hidden;
    margin-top: 2rem;
    border: 1px solid var(--border-color);
  }

  .table {
    margin-bottom: 0 !important;
  }

  .table thead {
    background: linear-gradient(135deg, var(--brand-primary), var(--brand-primary-dark)) !important;
  }

  .table thead th {
    color: white !important;
    font-weight: 600 !important;
    font-size: 0.875rem !important;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    padding: 1rem 1.25rem !important;
    border: none !important;
    white-space: nowrap;
    background: transparent !important;
  }

  .table tbody tr {
    transition: all 0.3s ease;
    border-bottom: 1px solid var(--border-color);
  }

  .table tbody tr:last-child {
    border-bottom: none;
  }

  .table tbody tr:hover {
    background-color: #fff5f5 !important;
    transform: translateX(4px);
  }

  .table tbody td {
    padding: 1.25rem !important;
    color: var(--text-primary);
    font-size: 0.95rem;
    vertical-align: middle;
    border: none !important;
  }

  .table tbody td:first-child {
    font-weight: 600;
    color: var(--text-primary);
  }

  .table tbody td:last-child {
    font-weight: 700;
    color: var(--brand-primary);
    font-size: 1.15rem;
  }

  .table-bordered tbody tr:nth-child(even) {
    background-color: rgba(254, 66, 77, 0.02) !important;
  }

  .table-bordered tbody tr:nth-child(even):hover {
    background-color: #fff5f5 !important;
  }

  @media (max-width: 768px) {
    .container {
      padding-top: 1rem;
    }

    h1 {
      font-size: 1.5rem;
    }

    .table-container {
      border-radius: 12px;
      overflow-x: auto;
    }

    .table {
      font-size: 0.85rem;
    }

    .table thead th,
    .table tbody td {
      padding: 0.75rem !important;
    }

    .table tbody tr:hover {
      transform: none;
    }

    .empty-state {
      padding: 3rem 1.5rem;
    }

    .empty-state-icon {
      font-size: 3rem;
    }

    .empty-state p {
      font-size: 1rem;
    }
  }
</style>

<div class="container mt-5">
  <h1>Dynamic Pricing for <%= listing?.title ?? "Listing" %></h1>

  <% if (!dynamicPrices || dynamicPrices.length === 0) { %>
    <div class="empty-state">
      <div class="empty-state-icon">💸</div>
      <p>No pricing data available for this listing.</p>
    </div>
  <% } else { %>
    <div class="table-container">
      <table class="table table-bordered mt-4">
        <thead>
          <tr>
            <th>Date</th>
            <th>Price (₹)</th>
          </tr>
        </thead>
        <tbody>
          <% dynamicPrices.forEach(day => { %>
            <tr>
              <td><%= day.date.toDateString() %></td>
              <td>₹<%= day.price.toFixed(2) %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>