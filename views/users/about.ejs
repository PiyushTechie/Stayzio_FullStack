<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Us - Stayzio</title>
    <link rel="stylesheet" href="/css/about.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="icon" type="image/png" href="/images/favicon-removebg-preview.png">
</head>
<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <span class="hero-badge">🏠 Your Home Away From Home</span>
                <h1>Revolutionizing Travel Stays</h1>
                <p class="hero-subtitle">At Stayzio, we believe every journey deserves an extraordinary place to rest. We're transforming how travelers discover, book, and experience accommodations worldwide.</p>
                <div class="hero-cta">
                    <a href="#mission" class="btn-primary">Our Mission</a>
                    <a href="#team" class="btn-outline">Meet the Team</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Mission Section -->
    <section class="mission-section" id="mission">
        <div class="container">
            <div class="section-header animate-on-scroll">
                <span class="section-badge">Our Mission</span>
                <h2 class="section-title">Building the Future of Travel</h2>
                <p class="section-subtitle">We're on a mission to make every stay memorable, comfortable, and authentic for travelers around the globe.</p>
            </div>

            <div class="mission-grid">
                <div class="mission-content animate-on-scroll">
                    <h3>Connecting People Through Travel</h3>
                    <p>Founded in 2025, Stayzio emerged from a simple yet powerful vision: to bridge the gap between travelers seeking authentic experiences and hosts wanting to share their unique spaces. What started as an idea to solve our own travel frustrations has grown into a platform trusted by thousands.</p>
                    
                    <p>We understand that where you stay can make or break your trip. That's why we've built a platform that doesn't just facilitate bookings – it creates connections, ensures quality, and delivers experiences that exceed expectations.</p>

                    <div class="mission-stats">
                        <div class="stat-item">
                            <span class="stat-number">10K+</span>
                            <span class="stat-label">Happy Travelers</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number">200+</span>
                            <span class="stat-label">Unique Properties</span>
                        </div>
                        <div class="stat-item">
                            <span class="stat-number">200+</span>
                            <span class="stat-label">Cities Worldwide</span>
                        </div>
                    </div>
                </div>

                <div class="mission-visual animate-on-scroll">
                    <div class="floating-elements">
                        <div class="floating-element">Discover</div>
                        <div class="floating-element">Connect</div>
                        <div class="floating-element">Experience</div>
                    </div>
                    <div class="mission-icon">🌍</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Values Section -->
    <section class="values-section">
        <div class="container">
            <div class="section-header animate-on-scroll">
                <span class="section-badge">Our Values</span>
                <h2 class="section-title">What Drives Us Forward</h2>
                <p class="section-subtitle">These core values guide every decision we make and every feature we build.</p>
            </div>

            <div class="values-grid">
                <div class="value-card animate-on-scroll">
                    <div class="value-icon">🎯</div>
                    <h3 class="value-title">Trust & Safety</h3>
                    <p class="value-description">We prioritize the safety and security of our community through rigorous verification processes, secure payments, and 24/7 support for both hosts and guests.</p>
                </div>

                <div class="value-card animate-on-scroll">
                    <div class="value-icon">✨</div>
                    <h3 class="value-title">Quality Excellence</h3>
                    <p class="value-description">Every property on our platform meets our high standards for cleanliness, amenities, and host responsiveness, ensuring consistently exceptional experiences.</p>
                </div>

                <div class="value-card animate-on-scroll">
                    <div class="value-icon">🤝</div>
                    <h3 class="value-title">Community First</h3>
                    <p class="value-description">We foster genuine connections between travelers and hosts, creating a global community where everyone feels welcomed and valued.</p>
                </div>

                <div class="value-card animate-on-scroll">
                    <div class="value-icon">🚀</div>
                    <h3 class="value-title">Innovation</h3>
                    <p class="value-description">We continuously evolve our platform with cutting-edge technology, AI-powered recommendations, and intuitive design to stay ahead of travel trends.</p>
                </div>

                <div class="value-card animate-on-scroll">
                    <div class="value-icon">🌿</div>
                    <h3 class="value-title">Sustainability</h3>
                    <p class="value-description">We promote responsible travel practices and support eco-friendly accommodations to minimize our environmental impact and preserve destinations for future generations.</p>
                </div>

                <div class="value-card animate-on-scroll">
                    <div class="value-icon">💎</div>
                    <h3 class="value-title">Transparency</h3>
                    <p class="value-description">Clear pricing, honest reviews, and open communication are fundamental to building trust and ensuring every booking meets expectations.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Team Section -->
    <section class="team-section" id="team">
        <div class="container">
            <div class="section-header animate-on-scroll">
                <span class="section-badge">Our Team</span>
                <h2 class="section-title">Meet the Visionaries</h2>
                <p class="section-subtitle">The passionate minds behind Stayzio's mission to transform travel experiences.</p>
            </div>

            <div class="team-grid">
                <div class="team-card animate-on-scroll">
                    <div class="founder-badge">Founder & CEO, CTO, Head of Operations</div>
                    <div class="team-avatar">👨‍💼</div>
                    <h3 class="team-name">Piyush Prajapati</h3>
                    <p class="team-role">Founder & Chief Executive Officer</p>
                    <p class="team-bio">Visionary entrepreneur with a passion for travel technology and creating meaningful connections. Piyush founded Stayzio with the mission to revolutionize how people experience accommodations worldwide, combining his expertise in business strategy with a deep understanding of traveler needs.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="container">
            <div class="cta-content animate-on-scroll">
                <h2 class="cta-title">Ready to Transform Your Travel?</h2>
                <p class="cta-description">Join thousands of travelers who've discovered extraordinary stays through Stayzio. Your perfect accommodation is just one click away.</p>
                <div class="cta-buttons">
                    <a href="/listings" class="btn-cta">Start Your Journey</a>
                </div>
            </div>
        </div>
    </section>
    <%- include("../includes/footer.ejs") %>

    <script>
        // Intersection Observer for animations
        const observerOptions = {
            threshold: 0.15,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('animated');
                }
            });
        }, observerOptions);

        // Observe all animation elements
        document.querySelectorAll('.animate-on-scroll').forEach(el => {
            observer.observe(el);
        });

        // Smooth scrolling for navigation
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Counter animation for stats
        function animateCounters() {
            const counters = document.querySelectorAll('.stat-number');
            const speed = 200;

            counters.forEach(counter => {
                const target = parseInt(counter.textContent.replace(/[^\d]/g, ''));
                const suffix = counter.textContent.replace(/[\d,+]/g, '');
                let count = 0;
                const increment = target / speed;

                const updateCount = () => {
                    if (count < target) {
                        count += increment;
                        counter.textContent = Math.floor(count).toLocaleString() + suffix;
                        requestAnimationFrame(updateCount);
                    } else {
                        counter.textContent = target.toLocaleString() + suffix;
                    }
                };

                updateCount();
            });
        }

        // Trigger counter animation when mission section is visible
        const missionObserver = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    animateCounters();
                    missionObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.5 });

        const missionSection = document.querySelector('.mission-stats');
        if (missionSection) {
            missionObserver.observe(missionSection);
        }

        // Enhanced hover effects for cards
        document.querySelectorAll('.value-card, .team-card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-8px) scale(1.02)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0) scale(1)';
            });
        });

        // Parallax effect for hero section
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const hero = document.querySelector('.hero');
            
            if (hero && scrolled < window.innerHeight) {
                hero.style.transform = `translateY(${scrolled * 0.5}px)`;
            }
        });

        // Dynamic color animations
        const valueCards = document.querySelectorAll('.value-card');
        valueCards.forEach((card, index) => {
            card.addEventListener('mouseenter', function() {
                this.style.animationDelay = `${index * 0.1}s`;
            });
        });

        // Add loading animation
        window.addEventListener('load', function() {
            document.body.style.opacity = '1';
        });

        // Mobile menu functionality (if needed)
        const mobileBreakpoint = 768;
        
        function handleResize() {
            if (window.innerWidth <= mobileBreakpoint) {
                // Mobile optimizations
                document.querySelectorAll('.floating-element').forEach(el => {
                    el.style.display = 'none';
                });
            } else {
                document.querySelectorAll('.floating-element').forEach(el => {
                    el.style.display = 'block';
                });
            }
        }

        window.addEventListener('resize', handleResize);
        handleResize(); // Initial check
    </script>
</body>
</html>